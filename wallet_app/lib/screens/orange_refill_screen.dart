import 'package:flutter/material.dart';

class OrangeRefillsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Orange Refills')),
      body: Center(child: Text('Orange Refills Page')),
    );
  }
}