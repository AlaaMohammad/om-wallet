import 'package:flutter/material.dart';

class TransferIBANScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Transfer to IBAN')),
      body: Center(child: Text('Transfer to IBAN Page')),
    );
  }
}