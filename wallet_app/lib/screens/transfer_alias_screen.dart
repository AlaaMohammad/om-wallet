import 'package:flutter/material.dart';

class TransferAliasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Transfer to Alias')),
      body: Center(child: Text('Transfer to Alias Page')),
    );
  }
}